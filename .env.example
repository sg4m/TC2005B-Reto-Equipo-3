# Frontend Environment Variables (Vite)
# Copy this file to .env.local (or .env) and replace placeholders with real values.

# Base URL for the backend API used by the frontend (development default)
VITE_API_BASE_URL=http://localhost:5000/api

# Optional: other frontend-specific keys
NODE_ENV=development

# DO NOT COMMIT REAL SECRETS - use .env.local for local development and set CI secrets for deployment