# Backend Environment Variables
# Copy this file to Backend/.env and replace the placeholders with real values.

# Server
PORT=5000
NODE_ENV=development

# PostgreSQL (used by the backend if not using Supabase)
# Provide either the direct DB_* values or configure via SUPABASE_* variables below
DB_HOST=your_db_host
DB_PORT=5432
DB_NAME=your_db_name
DB_USER=your_db_user
DB_PASSWORD=your_db_password

# Supabase (optional) - if you use Supabase, provide these instead
SUPABASE_URL=your_supabase_project_url
SUPABASE_ANON_KEY=your_supabase_anon_key

# Authentication & Security
JWT_SECRET=your_jwt_secret_key_here

# AI / Gemini (do not commit real keys)
GEMINI_API_KEY=your_gemini_api_key_here

# NOTE: Do NOT commit Backend/.env. Use this file as a template for teammates.